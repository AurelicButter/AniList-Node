/**
 * An enum for media seasons
 * @enum {String}
 * @readonly
 */
 
const MediaSeason = {
    WINTER: "WINTER",
    SPRING: "SPRING",
    SUMMER: "SUMMER",
    FALL: "FALL"
};

Object.freeze(MediaSeason);

module.exports = MediaSeason;